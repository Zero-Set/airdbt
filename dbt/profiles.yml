orange:
  outputs:

    dev:
      type: postgres
      threads: 1
      host: postgresdb
      port: 5432
      user: postgres
      pass: password
      dbname: postgres
      schema: postgres

  target: dev
